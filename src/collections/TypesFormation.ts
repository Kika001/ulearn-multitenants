import { CollectionConfig } from 'payload';

export const TypesFormation: CollectionConfig = {
  slug: 'typesFormation',
  fields: [
    {
      name: 'name',
      type: 'text',
      required: true,
    },
    // Ajoutez d'autres champs
  ],
};